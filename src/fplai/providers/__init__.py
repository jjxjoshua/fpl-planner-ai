"""Provider adapters (blueprint §12). See `providers/base.py` for the
interface every adapter satisfies, and `providers/fpl.py` for the worked
example — the FPL Official API ported onto it (E2b story 5)."""
