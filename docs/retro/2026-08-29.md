# Retro — session s005 (2026-08-29 → 2026-08-31)

Filename kept at the session's start date per the s005 punch-card's own convention. This
**supersedes** the mid-session partial version of this file (written 08-29, 12:25/12:36Z,
before Phase 3 existed) — it covers the whole session, including everything that landed after
that point: the rest of E5's follow-on fixes, and E6's prerequisites, MILP, backtest and ruling.

## 1. Shipped

Verified against `git log`, the working tree, and the punch-card — not accepted on report.

**E5 (Phase 2) — closed out cleanly:**
- Cards NONE/YELLOW isotonic layer built, **E5's one blocking condition on Phase 3 CLOSED**
  `08-29` (slopes 0.592→0.845, 0.630→0.901, calibrated strictly beats raw pooled).
- **GK saves — 7th model, gate PASSED** `08-30`. NB2 chosen on measurement; ships a *negative
  result* (its own isotonic layer measured harmful, `calibrate=False`) — the opposite default to
  cards, on its own evidence, and stated as such.
- **L2 penalty-scaling bug fixed in all seven models**, loss and gradient. Root cause of a
  symptom worked around three times previously. Real cause is **scale of `n`**, not feature
  scales — `PROGRESS.md` corrected to say so.
- **Isotonic saturation bug fixed** in minutes/cards/saves (Jeffreys `Beta(0.5,0.5)`
  correction) — minutes' 3.00%-of-predictions-at-exactly-0.0 problem, which had briefly made its
  own calibrator *worse* than raw on log-loss (+17%), is now +0.6% and its slope moved
  0.896→0.963. Saves had zero saturated bins, so its earlier `calibrate=False` call is
  re-confirmed on sound footing rather than coincidentally correct.
- Calibrator plumbing consolidated into `fplai.calibration`, proven bit-identical across all
  three models it replaced.
- Calibration report regenerated `08-30` (06:57Z) to reflect the L2 fix and fold saves in.
  **E5 still PASSES, now 20 outcomes (3 PASS / 15 PASS-with-accepted-departure / 2
  BASE-RATE-ONLY / 0 FAIL)** — see §3, this artifact is stale again as of the very next fix.

**E6 (Phase 3) — every prerequisite the s005 audit found missing now exists:**
- `src/fplai/scoring.py` — outcomes→points from live `game_config`, 610/610 GW1, 32/32 GW2.
- `src/fplai/points.py` — fixture-level seeded Monte Carlo PMF (rule 5), `highspy` added.
- `src/fplai/optimiser.py` — single-period MILP, 20 tests, constraint-correct, deterministic,
  captain a genuine decision variable, tie-break proven fail-first before being trusted.
- Live gameweek ingest: FPL is now a second provider of the existing
  `player.gameweek_stats@gameweek` capability; a union reader returns 180,560 rows.
  `was_home`/`opponent_team` leak — **caught live, not latent** (8 elements changed clubs in 9
  days, 5 rows concretely wrong) — fixed and re-ingested.
- **The E6 gate was run and is NOT genuinely attempted.** The MILP backtest used the identical
  trailing-4-GW proxy signal `GreedyFormBaseline` already uses, not the model stack — 3
  wins/3 losses vs template, net −23 across the three human-reference seasons, never within 130
  of the 2,146 career average. Architect ruling: record as neither pass nor failed-attempt.
  **The missing prerequisite — a prediction-time feature-assembly pipeline building
  `Sequence[PlayerFixtureFeatures]`** — is the project's single highest-value next story.

**Track A / user actions:** `THE_ODDS_API_KEY` rotated and the `snapshot_odds` scheduled task
registered, both by the user `08-29`, verified running unattended in production `08-30`
(hourly, deadline-gated, zero credits burned on no-op runs). GW2 in progress, transfer
Gibbs-White→Rogers confirmed on the API, captain Haaland.

### Process pattern, continued from the mid-session retro

**Routing is still collapsed to one lane.** Every substantive story in the back half of s005 —
GK saves, L2 fix, saturation fix, calibrator consolidation, scoring, points, optimiser, live
ingest — ran through `xl-coder`. `s-coder`, `debugger`, `data-scout` and `elite` did not run at
all in s005's second half. The mid-session retro's proposed remedy ("spawn Scrum Master at the
top of every session, not just at a perceived boundary") was itself not followed — this agent
is being spawned near the *end* of the session again, which is why the tracker went a full
session behind before being corrected here. Restating the remedy rather than declaring it
adopted: it has been recommended twice now and acted on zero times.

**The live-verification discipline held under real pressure.** Two agents this session hit
session-usage-limit resets mid-task; both had been writing punch-card findings incrementally
(lesson 15), so no measurement was lost even though compute was. The `PYTHONIOENCODING`
false-positive trap (finding at `.punchcard/s005.jsonl` line 207) is small but worth keeping —
a naive diff of two "identical" captured outputs on this Windows checkout reports spurious
differences on any non-ASCII line unless both captures share encoding.

## 2. The Architect's open questions

Checked against `docs/HANDOFF.md` §6, the prior retro's carried-forward list, and every
`finding`/`blocked`-shaped event in `.punchcard/s005.jsonl`.

**Still open, unresolved for multiple sessions:**
- **Q · Paid data.** Unchanged since 2026-08-19. The user offered to buy "a better API"
  (`docs/wiki/data-requirements.md`). Now several sessions untouched either way.
- **Q · `WakeToRun`.** Unresolved for a third session. `StartWhenAvailable` — the item three
  retros in a row wrongly recorded as `False` — turned out to be `True` all along; nobody had
  actually inspected the live task before this session. `WakeToRun` is the real remaining lever
  (wakes the PC at ~01:30 local for deadline-adjacent captures) and is explicitly the user's
  call, not the Architect's, because it changes the machine's behaviour. **Please answer this
  one directly rather than let it restate a fourth time** — the off-window it's meant to fix
  currently straddles the 01:30 local deadline.

**New this session, technical rather than user-facing, but genuinely undecided:**
- **DC's calibration-slope tradeoff** (`DEF_CBIT` 0.677→1.262 under the corrected L2 formula) —
  log-loss favours low `l2`, slope-near-1 favours `l2`≈10-100. Left at the unchanged `l2=1.0`
  default and flagged, not resolved either way.
- **`walk_forward_validate`'s `calibrate` default still differs across siblings** — `True` in
  `cards.py`, `False` in `minutes.py`, for the same parameter on sibling models. Named twice now
  (s004 retro and this session's punch-card), never acted on.
- **Two related-but-separate calibrators by design** (the walk-forward gate's 2-class binary
  one, the deployed 3-class model's) — escalated, not unified, still open.
- **Bonus's internal per-draw BPS array** — `_assign_bonus_points_batch` is private; a future
  Phase 5 wanting the full within-fixture joint (bonus correlated with that same draw's own
  minutes/goals) needs it exposed as a public seam. Named for the Architect, not attempted —
  low urgency (Phase 5), but worth carrying so it isn't rediscovered from scratch.

**Resolved this session — closing out items the last two retros carried forward:**
- `StartWhenAvailable=False`, carried unresolved across s003/s004/s005 as a live risk — was
  simply **wrong**. The task was correctly configured the whole time; the retro chain had been
  restating an unverified claim for three sessions running. Worth naming as its own lesson:
  *a finding repeated across retros without being re-checked against the live system is not
  corroborated, it's just unexamined.*
- `THE_ODDS_API_KEY` rotation + odds-capture scheduling, dropped from tracking in s002 and
  flagged as silently-dropped by the mid-session retro — both now done and verified live.
- Cards isotonic blocking condition, GK-saves prize measurement — both closed.
- "My before-capture doesn't match the brief's reference values" (`.punchcard/s005.jsonl` line
  203) — resolved same-session (line 198): two harnesses, different `min_train_rows`
  (`fit_minutes.py` 200 vs `calibration_report.py` 8000), not store drift. Any quoted metric
  must carry which harness produced it — three different agents were confused by this exact gap
  before it was pinned down.

## 3. Stale knowledge

- **`docs/wiki/calibration-report.md` — the E5 gate's own artifact — is stale as of the
  session's last fix.** It was regenerated `08-30` 06:57Z after the L2 fix, but the
  isotonic-saturation fix landed *after* that (22:10Z+) and was never folded back in. As it
  stands the file still narrates minutes' calibrated series as *worse* than raw on log-loss
  (0.3658, the pre-fix regression, complete with an "ARCHITECT-LEVEL FINDING" flagging whether
  `calibrate=True` should still hold) and cards NONE/YELLOW at the superseded 0.845/0.901
  rather than the final 0.875/0.931. No gate verdict is wrong — `beats_both_baselines()` held
  throughout — but a reader treating this page as current will see an alarming, already-resolved
  regression as live fact. One `scripts/calibration_report.py` re-run fixes it; not done here
  (out of owned paths), routed to XL-Coder or the Architect.
- **`docs/HANDOFF.md`'s own Phase 2 headline still reads "16 outcomes."`** The live report now
  scores 20 (7 models, saves folded in `08-30`). Not urgent — the composition (3 PASS / 15
  PASS-with-accepted-departure / 2 BASE-RATE-ONLY / 0 FAIL) is otherwise accurate — but the
  count is off by the seventh model's four outcomes. `PROGRESS.md`'s own E5 line is corrected
  in this pass; HANDOFF is the Architect's file, not fixed here.
- **`docs/wiki/provider-framework.md` §14.8 ("Recommended schedule — not created")** is now
  flatly contradicted: it says no Windows Scheduled Task exists and recommends a flat
  2-3-hourly cadence, while the shipped design is deadline-relative (T-78h/26h/6h/2h),
  registered, and verified running unattended in production. Flagged by the agent who wrote the
  superseding design (`.punchcard/s005.jsonl` line 43); never edited (out of that brief's owned
  paths). Still stale.
- **`docs/wiki/gw2-2026-27-intel.md`** carries its own self-declared expiry ("expires at the
  GW2 deadline, 01:30 Sat 29 Aug local") which has now passed — GW2 is in progress. Same
  pattern the GW1 file went through: start-probabilities and the Gibbs-White fitness read are
  stale on contact; the market/rotation findings remain valid history. Convert or discard per
  the GW1 precedent — XL-Coder or Data Scout's job, not fixed here.

## 4. User feedback

**Nothing new raised in s005's punch-card beyond confirmed actions.** The user completed two
standing asks unprompted mid-session — rotating `THE_ODDS_API_KEY` and registering the
`snapshot_odds` scheduled task via the Task Scheduler UI (the Architect could not do either:
the rotation needed the user's account, and `Register-ScheduledTask -LogonType Password`
prompts interactively for a Windows password the Architect must not enter). Both verified live
afterward rather than accepted on the user's word. No other direct feedback appears in this
session's log; the 08-29 target-reconfirmation and blueprint-amendment authority grant recorded
in `docs/HANDOFF.md` §5 predate this retro's window and are already correctly reflected there.

## Carried forward

Paid data (Q, unanswered) · `WakeToRun` (Q, third session, needs a direct answer) · DC's
calibration-slope tradeoff (l2=1.0 vs slope-near-1, unresolved) · `walk_forward_validate`
`calibrate` default mismatch across siblings · two related-but-separate calibrators (escalated,
not unified) · bonus's private per-draw BPS array (Phase 5 seam, named not built) ·
`docs/wiki/calibration-report.md` needs one more regeneration pass (saturation fix not folded
in) · `docs/HANDOFF.md`'s "16 outcomes" headline vs the live 20 · `docs/wiki/provider-framework.md`
§14.8 vs the shipped deadline-relative schedule · `gw2-2026-27-intel.md` conversion/discard ·
third duplication surface (`_log_loss`/`_brier`/`_calibration_slope_intercept`, 10 defs across 4
files) · six-model migration onto the union reader, must run after any gate-number regeneration
and must use the reader's `season` filter · `slow` pytest marker (suite now 1,062 tests,
~1,260s uncontended, still unsplit) · **GW2 EO sample, Tue 1 Sep evening local — irreversible if
missed** · GW3 loop, deadline 01:30 Sat 5 Sep local · GW4's early 20:30 Sat 12 Sep local deadline
· routing still collapsed onto one agent lane, recommendation restated a second time, not yet
acted on · **the E6 critical path — a prediction-time feature-assembly pipeline — is the
project's single highest-value next story and the next session's first move.**
