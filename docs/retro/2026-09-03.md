# Retro — 2026-09-03 — Phase 3 (E6) close

Audited cold, from `.punchcard/s006.jsonl` (full file, 190 events), `PROGRESS.md`, `git log`
on `feature/s006-capability-reader-migration` (HEAD `64381e5`), and `docs/wiki/optimiser.md`.
`docs/HANDOFF.md` was read only to check its own claim of currency (forbidden to edit). The
test suite and the gate were not re-run, per brief.

## 1. Shipped

**The gate claim reproduces exactly.** PROGRESS.md's E6 result table matches
`.punchcard/s006.jsonl:187-188` verbatim, and the arithmetic checks out independently:
mean `(2221+2392+2228)/3 = 2280.33`, template mean `(2092+1906+1951)/3 = 1983.0`, margins
129/486/277 all recompute correctly, and the "beats the proxy MILP by 273-359" claim also
recomputes (273/359/283 — 283 was mis-stated as within range but is correctly inside 273-359).
Commit `495a168` (13:06:28+08 / 05:06:28 UTC) is the code state the run executed against;
`64381e5` (15:43:09+08 / 07:43:09 UTC) records the result 2h36m later — consistent with "152
minutes" once the false-start (2022-23 unfittable, caught in 4 seconds) is subtracted.

**The caveats travel with the number, in `PROGRESS.md` itself, not only in commit messages** —
this was one of the brief's specific questions and the answer is yes: no transfer costs, three
seasons not four (with the reason — `starts` begins in 2022-23, not season-picking), the GW1 DC
cold-start, and E[points] vs rank-aware are all in the same block as the result table
(`PROGRESS.md:417-428`).

**A real leakage bug was caught and fixed before the gate ran, not after.** The gate-runner's
cold-start DC fallback initially reused a DC model fitted `as_of=now()` (informed by all of
2025-26 and 2026-27) for 2025-26 GW1, whose scoring config was *not* neutralised — a genuine
future-informs-past leak. Caught by the coordinator mid-task, fixed with a zero-data-dependency
degenerate DC bundle, and the fix's effect was measured, not assumed: the smoke-run total moved
90→85 after the fix, proving the leak had been live (`.punchcard/s006.jsonl:178-183`). The
unreachability of the leaky path is proven by injecting a `get_shared_dc` that raises if ever
called — an attack, not a happy-path test.

**The "already satisfied" and "not needed" closures are genuinely evidenced, not asserted.**
Historical `as_of`-correct assembly was closed against five named, existing tests that already
attack the leakage boundary (`.punchcard/s006.jsonl:136`). The scoreline-input story was
cancelled because `team_strength.predict_scoreline` already existed as a public entry point —
found by a 30-second probe, recorded as an Architect estimation error rather than silently
dropped (`:111`).

**Process held under stress.** Four leftover process-integrity findings from earlier in the
session (mis-encoded files, fabricated future timestamps, a hollow guard "proof") did not
recur in the E6-tail stories. Every one of the six later dispatches (fixture-schedule/attacking,
cards+DC+bonus, saves, composition entry point, leakage-boundary widening, frame-based
assembly, ModelStackStrategy, gate runner) shipped with zero removed top-level definitions
against its own baseline and a live-reproduced gate.

## 2. Open items carried out of the phase — as questions

**`build_squad` silently corrupts on duplicate element ids — `PROGRESS.md` E6, still `[ ]`.**
Not exercised today (`_candidates_from_attributes` emits one candidate per row), but Phase 4's
multi-period/DGW work is exactly where a per-fixture candidate list would first reach it, and
nothing raises. Is this deliberately queued for Phase 4's first DGW-adjacent story, or does it
need a story number before that work starts so it doesn't get rediscovered mid-dispatch the way
the DGW dedup non-determinism was?

**`docs/wiki/calibration-report.md` is stale from the *isotonic-saturation* fix (08-30 22:10Z),
still `[ ]` under E5, carried through this whole phase without being regenerated.** It is one
`scripts/calibration_report.py` re-run. Phase 4 will read this page as ground truth for model
behaviour it did not itself re-derive. Is regenerating it queued, or has "the verdict didn't
move, so it doesn't matter" become the working assumption without anyone saying so?

**Several E2/E2b items remain open** (2019-20/2022-23 backfill gaps, olbauday GrainPlan,
heartbeat not wired into `snapshot_odds.py`/`sample_picks.py`). None of these block E6 and none
were touched this phase — flagged only because a phase close is a natural checkpoint to ask
whether they are still wanted, or should be formally deferred rather than left open by default.

## 3. Stale knowledge

**`docs/wiki/optimiser.md` §4 makes a claim the session's own record falsified.** It states:
*"When the live/historical feature pipeline lands, `MILPStrategy` swaps its one call
(`_trailing_points_distributions` → a per-fixture `simulate_fixture_points_pmfs` call) —
nothing else in this module changes."* The Architect's own punch-card entry explicitly
overturns this: *"THE (3) MILP SWAP IS ~13, AND TWO PROBES SHOWED IT... true only of
`optimise_squad`'s INTERFACE"* (`.punchcard/s006.jsonl:133`) — the actual work was a new
`ModelStackStrategy` class plus schedule facts added to `ATTRIBUTE_COLUMNS`, a new
`GameweekView.fixtures` field, DGW convolution, and six frame-based assemblers, none of which
existed when §4 was written. The page was never updated. A reader trusting it would
underestimate Phase 4-adjacent optimiser work by roughly the same margin the Architect did.

**`PROGRESS.md`'s own header (lines 3-9) contradicted its own E6 section before this audit.**
It read "Gate:... Not yet genuinely attempted — the MILP backtest ran on a trailing-points
proxy, not the model stack" and "Phase 3 (E6) IN PROGRESS," while the E6 section 130+ lines
below it documents the gate as passed with a full evidence table. This is corrected below as
part of setting the phase-done mark (my owned scope), not left for the Architect to trip over.

**`docs/HANDOFF.md` is stale in the way the brief said it would be, confirmed by reading it.**
Its banner still reads *"§3.5... explains why E6 is neither passed nor failed"* and *"nothing
can yet produce a prediction for any gameweek"* — both were true on 31 Aug and are now false.
It is the first file a new session reads. Forbidden to me; flagged for the Architect.

**`PROGRESS.md`'s own `[!]` line under E6** ("E6's gate was run but NOT genuinely attempted...
record as neither passed nor failed-attempt") is still present, unedited, below the passed-gate
result table that supersedes it. Not touched here — per-story lines are the Architect's under
dispatch-protocol rule 9 — but flagged because a top-to-bottom reader hits the contradiction
before reaching the resolution.

**No full (slow-inclusive) pytest run is on record for the code actually at HEAD.** The last
full-suite number anywhere in the record is `1340 passed in 1634.39s`, committed in `48612b2`'s
message (arithmetically consistent with that story's own `1275 passed, 65 deselected` fast-loop
result — not fabricated) — but that commit is **three stories and one leak-fix behind HEAD**
(schedule-facts boundary, DGW handling, ModelStackStrategy, the gate runner and its
post-correction round all landed afterward). Every later story ran only its own slow tests plus
the repo-wide `-m "not slow"` fast loop (last: `1302 passed, 71 deselected`). `PROGRESS.md`'s
headline "Suite: 1,340 tests, green" line is stale and internally inconsistent with the fast-loop
figure quoted one sentence later in the same file (`1284 passed, 66 deselected` → true full count
would be 1350, not 1340). The brief instructed me not to re-run the suite because "both results
are on record" — for the gate itself that's true; for a whole-suite green claim at current HEAD,
it is not. This does not touch the gate's own evidence (a separate, independently-run harness)
and is not grounds to withhold, but the claim as currently worded overstates what was verified.

## 4. User feedback

None recorded in the punch-card for this phase close — the only user-attributed exchange in
`s006` is the vaastav-directory question about historical scoring rules
(`.punchcard/s006.jsonl:124`), which the Architect answered and which is not feedback on this
phase's process. Nothing to report here; not inventing any.

## 5. Verdict

**PHASE DONE.**

The gate condition named in the brief — beat the template over 2+ backtested seasons, ~2,100
points, ideally 2,146 — is met with a wide margin (lowest season 2,221, mean 2,280.3) on
evidence that reproduces independently from the punch-card and the commit history: a named
seed, a named commit, a reproducible run duration, and caveats recorded in the same file as the
result rather than only in a commit message. The one serious risk to the number's integrity — a
future-informs-past DC leak in the gate runner's cold-start fallback — was found and closed
*before* the recorded run, with a measured behavioural proof (90→85) and an unreachability
attack, which is the standard this project holds itself to for exactly this class of claim.

Findings 3's five items are real but none of them undermine the gate's own evidence chain; they
are documentation and tracker hygiene that a phase close is the right moment to fix. Per my
owned scope (phase-summary section and phase-done mark only), I have:

- Corrected `PROGRESS.md`'s header so it no longer contradicts its own E6 section.
- Appended a phase-summary block closing E6.

I have **not** touched `docs/wiki/optimiser.md`, `docs/HANDOFF.md`, or any individual E6 story
line (including the dangling `[!]`) — those are outside my owned paths and are routed to the
Architect via section 2 and 3 above.
