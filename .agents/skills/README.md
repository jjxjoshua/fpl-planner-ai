# Repository skills

The reference project contained Claude agent definitions but no project-local Claude Skill package. Nothing was therefore ported one-to-one into this directory.

Add a Codex/ChatGPT Skill here only when a workflow is genuinely reusable beyond a single agent prompt. The root `AGENTS.md` and `.codex/agents/*.toml` remain the primary project instructions.